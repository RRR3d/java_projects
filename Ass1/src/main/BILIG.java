package main;

import javax.swing.*;

public class BILIG {
    public static void main(String[] args) {
        /*
        BILIG b = new BILIG();
        int ID = b.getID().length();
        if( ID == 10){
            double gpa = b.getGPA();
            if( 0 < gpa && gpa < 4){ // check if the gpa is between 0 and 4
                String fac = b.getFaculty(); // GET THE FACulty
                String dep1 = b.getDep();  // GET The DEPartment
                Student s1 = new Student(ID , dep1 , fac , gpa); // create a new Student
                JOptionPane.showMessageDialog(null , s1);
            }
            else
                JOptionPane.showMessageDialog(null , "please enter a correct GPA! ");
        }
        JOptionPane.showMessageDialog(null , "please enter a correct ID!");
    }







    // GET GPA method
    public double getGPA(){
        String inputGPA = JOptionPane.showInputDialog(null , "Please enter your GPA" , "BILGI SIS" ,
                JOptionPane.QUESTION_MESSAGE);
        // Convert the String into int
        double gpa =  Double.parseDouble (inputGPA);
        return gpa;
    }

    // GET FACULTY method
    public String getFaculty(){
        String[] Fuc = {"FACULTY OF ENG" , "FACUALTY OF BUS"};
        String inputFuc = (String) JOptionPane.showInputDialog(null , "Please choose your faculty: ",
                "BILGI" , JOptionPane.QUESTION_MESSAGE , null , Fuc , Fuc[0]);
        return inputFuc;
    }

    // GET ID method
    public String getID(){
        String inputID = JOptionPane.showInputDialog(null , "please enter your ID:" ,
                "BILGI ", JOptionPane.QUESTION_MESSAGE);
        return inputID;
    }

    // GET DEP METHOD
    public String getDep() {
        String[] dep = {"CMPE", "MECHA"};
        String inputFuc = (String) JOptionPane.showInputDialog(null, "Please choose your faculty: ",
                "BILGI", JOptionPane.QUESTION_MESSAGE, null, dep, dep[0]);
        return inputFuc;
    }

         */

}}
